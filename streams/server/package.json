{
  "name": "server",
  "version": "1.0.0",
  "description": "a fake REST API server with JSON Server Package",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3001 --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yixing Wang",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
